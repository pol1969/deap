# coding: utf-8
import numpy ad np
import numpy as np
np
import datetime as dt
dt.now()
dt.datetime(2020,11,1)
d = dt.datetime(2020,11,1)
d
n = np.datetime64(d)
n
n = np.datetime64(d,'nat')
n = np.datetime64('nat')
n
n = d
n
d
d = datetime.date(2020,11,1)
d = dt.date(2020,11,1)
d
n = np.datetime64(d)
n
dd = np.arange(n,10)
dd
dd.reshape(-1,1)
